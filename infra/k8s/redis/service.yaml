apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: helionest
  labels:
    app: redis
    app.kubernetes.io/part-of: helionest-ai
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
