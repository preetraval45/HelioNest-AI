# ⚠️  DO NOT commit real secrets to git.
# Replace all placeholder values before applying.
# For production, use an external secret manager (AWS Secrets Manager, Vault, Sealed Secrets).
#
# To base64-encode a value:  echo -n "myvalue" | base64
#
apiVersion: v1
kind: Secret
metadata:
  name: helionest-secrets
  namespace: helionest
  labels:
    app.kubernetes.io/part-of: helionest-ai
type: Opaque
data:
  # echo -n "helionest" | base64
  DB_PASSWORD: aGVsaW9uZXN0
  # echo -n "change-me-to-a-long-random-string" | base64
  SECRET_KEY: Y2hhbmdlLW1lLXRvLWEtbG9uZy1yYW5kb20tc3RyaW5n
  # Add your real API keys below (base64-encoded)
  ANTHROPIC_API_KEY: ""
  GEOCODIO_API_KEY: ""
  NREL_API_KEY: ""
  MAPBOX_TOKEN: ""
  NEXT_PUBLIC_MAPBOX_TOKEN: ""
