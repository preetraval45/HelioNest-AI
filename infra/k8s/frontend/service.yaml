apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: helionest
  labels:
    app: frontend
    app.kubernetes.io/part-of: helionest-ai
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
